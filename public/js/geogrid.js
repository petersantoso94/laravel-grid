
document.addEventListener("DOMContentLoaded", function(event) { 
    var options = {
        cellHeight: 100,
        verticalMargin: 10,
        animate: true
    }, $ = jQuery;
    $('.grid-stack').gridstack(options);
  });